
let DELAYED_None	= 0
let DELAYED_Always	= 1
let DELAYED_Taken	= 2

extend
	beq, bne,
	blt, bltu,
	bge, bgeu
		otawa_target = NPC + tgt_offset
		otawa_delayed = DELAYED_Always

extend
	jal, jalr
		otawa_target = NPC + tgt_offset;
		otawa_delayed = DELAYED_Always
